# =============================================================================
# Task Manager Pro - Environment Configuration
# =============================================================================
# Copy this file to .env and update the values as needed
# Command: cp .env.example .env
#
# NOTE: Never commit the .env file to version control. It's already in .gitignore
# =============================================================================

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------

# Port number for the backend API server
# Default: 3001
# The frontend Vite dev server runs on port 3000
PORT=3001

# Node environment (development, production, or test)
# Default: development
# - development: Enables detailed logging, hot reload
# - production: Optimized for performance, serves built frontend
# - test: For running automated tests
NODE_ENV=development

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------

# SQLite database file path
# Default: dist/data/tasks.db (created automatically)
# Uncomment and customize if you want a different location
# DB_PATH=./data/tasks.db

# Note: The database file and directory are created automatically on first run
# The schema includes: tasks table with indexes for optimal performance

# -----------------------------------------------------------------------------
# CORS Configuration
# -----------------------------------------------------------------------------

# Allowed origin for Cross-Origin Resource Sharing (CORS)
# Default: http://localhost:3000
# In production, set this to your frontend domain (e.g., https://yourdomain.com)
CORS_ORIGIN=http://localhost:3000

# For multiple origins, you'll need to modify server.ts cors configuration

# -----------------------------------------------------------------------------
# Security Configuration
# -----------------------------------------------------------------------------

# Rate Limiting:
# - Window: 15 minutes
# - Max requests per IP: 100 requests per window
# - Configured in server.ts (not environment variables)

# Helmet Security Headers:
# - Enabled by default in production
# - CSP disabled for development (configure in server.ts for production)

# -----------------------------------------------------------------------------
# Optional Production Settings (Uncomment when deploying)
# -----------------------------------------------------------------------------

# Production port (if different from development)
# PORT=8080

# Production CORS origin
# CORS_ORIGIN=https://yourdomain.com

# Production database path
# DB_PATH=/var/data/tasks.db

# -----------------------------------------------------------------------------
# Development Tips
# -----------------------------------------------------------------------------

# 1. Frontend dev server: http://localhost:3000
# 2. Backend API server: http://localhost:3001
# 3. API endpoint: http://localhost:3001/api/tasks
# 4. Run dev servers: npm run dev
# 5. Build for production: npm run build
# 6. Start production: npm start

# -----------------------------------------------------------------------------
# Database Backup
# -----------------------------------------------------------------------------

# Use the provided backup script: ./scripts/db-backup.sh
# Backups are saved to: data/backups/

# =============================================================================
